# golden-01-arranque, lado PORT -- e a assimetria e temporaria, com prazo.
#
# O par deste arquivo e golden-01-arranque.txt. Eles deveriam ser o MESMO
# roteiro, e serao: roteiro que diverge entre os lados deixa de ser a mesma
# entrada, que e a condicao de qualquer comparacao valer. Hoje nao dao, por
# duas medidas:
#
# 1. ~~**o port nao abre imagem.**~~ **Resolvido na WTE-TASK-25**: o
#    `MainForm.FormShow` carrega a imagem que o `golden_run_laz.sh` sempre
#    passou como argumento. O que sobra e outra assimetria, menor: o oraculo
#    recebe o caminho pelo dialogo `Abre` e o port pela linha de comando, entao
#    o bloco de abertura do roteiro do oraculo nao tem par aqui;
# 2. ~~**o port nao recebe teclado no `:99`.**~~ **Falso desde a
#    WTE-TASK-26**, e a recusa que o `golden_run_laz.sh` fazia por causa disso
#    saiu. A WTE-TASK-13 mediu que a tecla nao chegava; o que faltava era
#    `xdotool windowfocus` antes -- `XSetInputFocus`, que nao precisa de
#    gerenciador de janela. Com `ROTEIRO_FOCO=1`, 3 `! tecla Down` produzem 3
#    disparos de `lista_equiposChange` no trace do port.
#
# Enquanto o item 1 valer, o lado port so abre a janela e espera, e as faixas
# que o oraculo grava ficam declaradas como `conhecida:` no roteiro do oraculo.
# Unificar os dois e trabalho da WTE-TASK-27 ou de quem primeiro precisar de um
# roteiro que edite pelos dois lados -- que e o formato que a
# WTE-TASK-26 pede para julgar edicao.
alvo: port
estado: ok
operacao: arranque

# O titulo tem o sufixo ` [Lazarus]`, posto em tempo de execucao pelo
# `wtemain.pas`. E divergencia deliberada e obrigatoria: os dois lados rodam no
# MESMO :99, e com titulo igual o harness dirigiria o lado errado.
> W11 Team Editor PT by chagas_michel! \[Lazarus\]
~ 3
