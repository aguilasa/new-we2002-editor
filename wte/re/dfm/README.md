# `re/dfm/` — os 18 formulários em texto

Vazio até a **WTE-TASK-03**, que produz o `dfm_extract.py` e despeja aqui os 18
DFM do `.rsrc` do `we-team-editor.exe`, convertidos de binário para texto.

Saída de gerador: **não editar à mão.** Correção entra no `dfm_extract.py` e o
diretório é regerado.
